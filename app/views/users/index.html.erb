<h1>TRADERS</h1>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Email</th>
            <th>Role</th>
            <th>Status</th>
            <th></th>
        </tr>
    </thead>


    <tbody>
        <% @users.each do |user| %>
            <%= user.approved %>
            <% if user.role == 'trader' %>
                <tr>
                    <td><%= user.id %></td>
                    <td><%= user.email %></td>
                    <td><%= user.role %></td>
                    <% if user.approved == true %>
                        <td>Approved</td>
                    <% elsif %>
                        <td>Pending Approval</td>
                    <% end %>
                    <td><%= link_to "Approve Trader", edit_user_path(user) %></td>
                </tr>
            <% end %>
        <% end %>
    </tbody>
</table>